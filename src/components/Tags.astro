---
const { tags } = Astro.props
---

<span>
    { tags.map((tag, index) => 
    <>
        <a class="tag-link" href={`/links/${tag}/`}>#{tag}</a>{index < tags.length - 1 ? ', ' : ''}
    </>
    )}
</span>

